# pytest.ini
[pytest]
minversion = 7.0
addopts = -ra -q --disable-warnings --cov=scripts --cov-report=term-missing
testpaths = tests
pythonpath = .
markers =
    integration: marks tests as integration (run on Databricks)
    unit: marks tests as unit (local)
